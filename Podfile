# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'watcha-giphy' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Giphy'
  # Pods for watcha-giphy
  pod 'Alamofire', '~> 5.4'
  
  target 'watcha-giphyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'watcha-giphyUITests' do
    # Pods for testing
  end

end
